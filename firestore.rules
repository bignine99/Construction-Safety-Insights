
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to the incidents collection.
    // This allows your app to display the data.
    match /incidents/{incidentId} {
      allow read: if true;
      allow write: if false; // Disallow writes from the browser for security.
    }
  }
}
