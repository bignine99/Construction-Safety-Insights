rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to read the incidents data for the dashboard.
    match /incidents/{incidentId} {
      allow read: if true;
      allow write: if false; // Disallow writes from the browser for security.
    }
  }
}
